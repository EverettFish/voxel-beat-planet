interface:
  display_name: "Voxel Beat Planet"
  short_description: "Build bespoke reactive voxel planets from songs"
  brand_color: "#E84C2A"
  default_prompt: "Use $voxel-beat-planet to turn this song or MP3 into a bespoke interactive voxel planet with authored landmarks, travelling beat waves, and a climate timeline."
policy:
  allow_implicit_invocation: true
